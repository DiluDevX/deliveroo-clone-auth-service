name: Release Develop

on:
  push:
    branches:
      - develop

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: development
    secrets: inherit
