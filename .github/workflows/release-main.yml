name: Release Main

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
    secrets: inherit
