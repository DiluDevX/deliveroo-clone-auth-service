name: Release Main

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Deploy to Production
    uses: ./.github/workflows/deploy.yml
    with:
      environment: production
    secrets: inherit
